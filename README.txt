1- Run server.py and client.py files
2- While client.py is running you will need to enter 2 inputs, file_path and file_name
	file_path = the path where to find the file that you want to enconded and send to server
	file_name = will be set as the new file name on the server
3- inside client directory has 3 file which you can make tests (test_with_pdf, test_with_txt and now_with_docx)
4- The new document will be created into server directory
5- you can type exit to quit